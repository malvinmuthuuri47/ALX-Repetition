#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: non-zero value on success and EOF on error
 */
int main(void)
{
	char str[] = "Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
